server:
  port: ${PORT:8085}
spring:
  config:
    import: classpath:backend-defaults.yml
  application:
    name: RatingService
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/ratings}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
