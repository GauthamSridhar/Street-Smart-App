spring:
  datasource:
    url: ${TEST_DB_URL:jdbc:h2:mem:ratings;MODE=PostgreSQL;DB_CLOSE_DELAY=-1}
    username: ${TEST_DB_USER:sa}
    password: ${TEST_DB_PASSWORD:}
  cloud:
    discovery:
      enabled: false
eureka:
  client:
    enabled: false
security:
  jwt:
    secret: dGVzdC1vbmx5LXN0cmVldC1zbWFydC1rZXktMzItYnl0ZXM=
  internal-key: test-only-internal-key-32-characters-long
delivery:
  enabled: false
sms:
  enabled: false
