// Public settings only. Browser map keys must be restricted by referrer and API.
// Leave the key empty to use product discovery without Google Maps.
window.streetSmartConfig = { apiBaseUrl: '/api', googleMapsApiKey: '' };
