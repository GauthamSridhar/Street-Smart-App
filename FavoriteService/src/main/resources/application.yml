server:
  port: ${PORT:8086}
spring:
  config:
    import: classpath:backend-defaults.yml
  application:
    name: FavoriteService
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/favorites}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
